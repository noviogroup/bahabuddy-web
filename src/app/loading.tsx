import { PageLoading } from '@/components/ui'

/** Root loading UI for marketing and public routes during navigation. */
export default function RootLoading() {
  return <PageLoading minHeight="min-h-screen" className="bg-offwhite" />
}
